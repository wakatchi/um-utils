{
    "scripts": {
        "wp-env": "wp-env",
        "env:start": "wp-env start --xdebug=develop,coverage --update --coverage-clover coverage.xml .",
        "env:stop": "wp-env stop",
        "test:run": "wp-env run --env-cwd='wp-content/themes/test' tests-wordpress vendor/bin/phpunit --verbose --coverage-html tests-result"
    },
    "devDependencies": {
        "@wordpress/env": "^8.13.0"
    },
    "config": {
        "WP_DEBUG": true
    }
}
